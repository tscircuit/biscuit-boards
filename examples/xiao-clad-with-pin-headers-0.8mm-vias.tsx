import { XiaoCladWithPinHeaders08mmVias } from "../lib/xiao-clad-0.8mm-vias"

/** XIAO form-factor clad preview with standard headers and 0.8 mm vias. */
export default () => (
  <XiaoCladWithPinHeaders08mmVias routingDisabled markHeadersNoConnect />
)
