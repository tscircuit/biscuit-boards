import { BreadboardClad } from "../lib/breadboard-clad"

/** Bare breadboard clad preview with every socket explicitly unconnected. */
export default () => <BreadboardClad routingDisabled markHeadersNoConnect />
