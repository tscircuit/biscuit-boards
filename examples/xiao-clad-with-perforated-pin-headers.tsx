import { XiaoCladWithPerforatedPinHeaders } from "../lib/xiao-clad-with-perforated-pin-headers"

/** Bare XIAO clad with perforated pin-header side edges. */
export default () => (
  <XiaoCladWithPerforatedPinHeaders routingDisabled markHeadersNoConnect />
)
